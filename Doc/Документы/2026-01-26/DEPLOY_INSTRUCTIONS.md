# 🚀 Инструкция по деплою на сервер 46.224.27.19:3000

## 📋 Быстрый старт

### Вариант 1: Автоматический деплой (рекомендуется)

```bash
# 1. Запустить скрипт деплоя
./deploy-to-server.sh
```

Скрипт автоматически:
- ✅ Проверит SSH подключение
- ✅ Проверит доступность порта 3000
- ✅ Создаст резервную копию (если проект существует)
- ✅ Остановит старый PM2 процесс
- ✅ Загрузит и установит проект
- ✅ Соберет проект
- ✅ Запустит через PM2 на порту 3000

### Вариант 2: Ручной деплой

```bash
# 1. Подключиться к серверу
./connect-server.sh

# Или напрямую:
ssh root@46.224.27.19
```

На сервере выполнить:

```bash
# 1. Перейти в директорию проекта
cd /var/www/wl

# 2. Остановить старый процесс (если есть)
pm2 stop wl-shop || true
pm2 delete wl-shop || true

# 3. Обновить код (если используется Git)
git pull origin main

# 4. Установить зависимости
npm install --production

# 5. Сгенерировать Prisma клиент
npx prisma generate

# 6. Удалить кэш
rm -rf .next

# 7. Собрать проект
npm run build

# 8. Создать/обновить .env файл
cat > .env << 'EOF'
NODE_ENV=production
PORT=3000
NEXTAUTH_URL=http://46.224.27.19:3000
NEXTAUTH_SECRET=wl-shop-secret-key-$(date +%s)
DATABASE_URL=postgresql://postgres:password@localhost:5432/wl_shop?schema=public
NEXT_PUBLIC_API_URL=http://46.224.27.19:3000/api
NEXT_PUBLIC_SITE_URL=http://46.224.27.19:3000
EOF

# 9. Создать конфигурацию PM2
cat > ecosystem.config.js << 'EOF'
module.exports = {
  apps: [{
    name: 'wl-shop',
    script: 'npm',
    args: 'start',
    cwd: '/var/www/wl',
    env: {
      NODE_ENV: 'production',
      PORT: 3000
    }
  }]
};
EOF

# 10. Запустить через PM2
pm2 start ecosystem.config.js
pm2 save

# 11. Проверить статус
pm2 status
pm2 logs wl-shop
```

---

## 🔍 Проверка статуса

```bash
# Проверить статус сервера и проекта
./check-server-status.sh
```

Или вручную:

```bash
# Проверить SSH подключение
ssh root@46.224.27.19 "echo 'SSH OK'"

# Проверить порт 3000
ssh root@46.224.27.19 "netstat -tlnp | grep :3000"

# Проверить PM2 процесс
ssh root@46.224.27.19 "pm2 status"

# Проверить доступность сайта
ssh root@46.224.27.19 "curl -I http://localhost:3000"
```

---

## 🌐 Доступ к проекту

После успешного деплоя проект будет доступен по адресу:

**http://46.224.27.19:3000**

---

## 📊 Управление PM2

```bash
# Подключиться к серверу
ssh root@46.224.27.19

# Просмотр статуса
pm2 status

# Просмотр логов
pm2 logs wl-shop

# Перезапуск
pm2 restart wl-shop

# Остановка
pm2 stop wl-shop

# Удаление из PM2
pm2 delete wl-shop

# Мониторинг в реальном времени
pm2 monit
```

---

## 🔧 Устранение проблем

### Проблема: "Порт 3000 занят"

**Решение:**
```bash
# Проверить что использует порт
ssh root@46.224.27.19 "netstat -tlnp | grep :3000"

# Остановить процесс (если нужно)
ssh root@46.224.27.19 "pm2 stop <название_процесса>"
```

### Проблема: "SSH подключение не работает"

**Решение:**
1. Проверить SSH ключи: `ls -la ~/.ssh/`
2. Добавить ключ на сервер: `ssh-copy-id root@46.224.27.19`
3. Проверить подключение: `ssh root@46.224.27.19`

### Проблема: "Проект не запускается"

**Решение:**
```bash
# Проверить логи
ssh root@46.224.27.19 "pm2 logs wl-shop --lines 50"

# Проверить переменные окружения
ssh root@46.224.27.19 "cd /var/www/wl && cat .env"

# Пересобрать проект
ssh root@46.224.27.19 "cd /var/www/wl && rm -rf .next && npm run build"
```

### Проблема: "База данных не подключается"

**Решение:**
```bash
# Проверить подключение к БД
ssh root@46.224.27.19 "cd /var/www/wl && npx prisma db pull"

# Проверить переменную DATABASE_URL
ssh root@46.224.27.19 "cd /var/www/wl && cat .env | grep DATABASE_URL"
```

---

## 📝 Обновление проекта

Когда нужно обновить код:

```bash
# 1. Запустить скрипт деплоя
./deploy-to-server.sh
```

Или вручную:

```bash
# 1. Подключиться к серверу
ssh root@46.224.27.19

# 2. Перейти в директорию проекта
cd /var/www/wl

# 3. Обновить код (если используется Git)
git pull origin main

# 4. Переустановить зависимости (если нужно)
npm install --production

# 5. Пересобрать проект
rm -rf .next
npm run build

# 6. Перезапустить PM2
pm2 restart wl-shop
```

---

## ✅ Checklist деплоя

- [ ] SSH ключи настроены
- [ ] Порт 3000 доступен (или освобожден)
- [ ] Проект собран локально (`npm run build`)
- [ ] База данных настроена на сервере
- [ ] Переменные окружения (.env) настроены
- [ ] PM2 установлен на сервере
- [ ] Проект запущен через PM2
- [ ] Сайт доступен по http://46.224.27.19:3000

---

## 🔐 Безопасность

**⚠️ ВАЖНО:**
- Никогда не коммитьте `.env` файлы в Git
- Используйте сильные пароли для базы данных
- Регулярно обновляйте зависимости
- Настройте firewall на сервере
- Используйте HTTPS в продакшене (через Nginx + Let's Encrypt)

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `pm2 logs wl-shop`
2. Проверьте статус: `pm2 status`
3. Проверьте порт: `netstat -tlnp | grep :3000`
4. Проверьте .env файл: `cat /var/www/wl/.env`

---

**Создано:** 2025-02-07  
**Версия:** 1.0  
**Статус:** ✅ Готов к использованию

