# Анализ файлов в public/images/

## 📋 Текущее состояние

### Критичные файлы (должны остаться в public/):

1. **`logo.png`** - используется в:
   - ✅ Header (desktop и mobile) - критично для навигации
   - ✅ Footer
   - ✅ `manifest.json` (PWA иконки) - критично для PWA
   - ✅ `layout.tsx` (metadata icons) - критично для SEO
   - ✅ Service Worker (`sw.js`) - критично для PWA
   - **Вывод:** ОСТАВИТЬ в public/

2. **`logo.ico`** - favicon
   - ✅ Используется в `layout.tsx`
   - **Вывод:** ОСТАВИТЬ в public/

3. **`star.png`** - используется в CSS
   - ✅ `globals.css` - `mask-image: url('/images/star.png')`
   - **Вывод:** ОСТАВИТЬ в public/

### Не критичные файлы (можно переместить в Blob):

1. **Дефолтные изображения категорий** (армянские .JPG):
   - `Օրորոցներ.JPG`, `Պահարաններ.JPG`, и т.д.
   - Используются только как fallback, если в БД нет изображения
   - **Вывод:** Можно переместить в Blob, но не обязательно

2. **`quyrikneri-nkar.webp`** - только для страницы "О нас"
   - **Вывод:** Можно переместить в Blob

3. **`Group 465918.png`** - только для footer
   - **Вывод:** Можно переместить в Blob

---

## ✅ Рекомендация: Гибридный подход

### ОСТАВИТЬ в public/ (критичные статические файлы):
- ✅ `logo.png` - критично для работы приложения
- ✅ `logo.ico` - favicon
- ✅ `star.png` - используется в CSS

**Почему:**
- Кешируются CDN Next.js автоматически
- Доступны сразу без API запросов
- Не требуют токенов
- Быстрее загружаются
- Критичны для работы приложения

### МОЖНО переместить в Blob (не критичные):
- Дефолтные изображения категорий (армянские .JPG)
- `quyrikneri-nkar.webp`
- `Group 465918.png`

**Но:** Это не обязательно, так как:
- Они не меняются часто
- Не критичны для работы приложения
- Проще оставить в public/ для простоты

### ХРАНИТЬ в Blob (пользовательские загруженные):
- ✅ Все новые загруженные изображения товаров
- ✅ Загруженные логотипы через админ-панель
- ✅ Пользовательский контент

---

## 🎯 Итоговая рекомендация

**ОСТАВИТЬ текущую структуру:**
- Критичные статические файлы в `public/images/`
- Пользовательские загруженные файлы в Blob Storage

**Почему это правильно:**
1. ✅ Стандартная практика Next.js
2. ✅ Оптимальная производительность
3. ✅ Простота поддержки
4. ✅ Не требует дополнительных API запросов для критичных файлов
5. ✅ Автоматическое кеширование CDN

**Не нужно:**
- ❌ Перемещать все в Blob (это усложнит и замедлит)
- ❌ Удалять public/images/ (критичные файлы должны быть там)

---

## 📝 Вывод

**Текущая структура правильная!** Не нужно ничего менять.

- `public/images/` - для критичных статических файлов
- Blob Storage - для пользовательских загруженных файлов

Это оптимальный и правильный подход. ✅
