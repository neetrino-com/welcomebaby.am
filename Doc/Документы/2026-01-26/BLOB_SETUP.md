# Настройка Vercel Blob Storage

## 📋 Общая информация

Проект использует **Vercel Blob Storage** для хранения файлов как в локальной разработке, так и на production. Оба окружения работают одинаково с Blob Store.

---

## 🔑 Получение токена для локальной разработки

### Шаг 1: Создать Blob Store в Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Перейдите в ваш проект (или создайте новый)
3. Откройте вкладку **Storage**
4. Нажмите **Create** → выберите **Blob**
5. Введите имя: `welcomebaby-files` (или любое другое)
6. Выберите регион (рекомендуется тот же, что и проект)
7. Нажмите **Create**

### Шаг 2: Создать токен

1. Откройте созданный Blob Store
2. Перейдите на вкладку **Tokens**
3. Нажмите **Create Token**
4. Введите имя токена: `local-development` (или любое другое)
5. Выберите права: **Read and Write**
6. Нажмите **Create**
7. **Скопируйте токен** (он показывается только один раз!)

### Шаг 3: Добавить токен в .env

Откройте `.env` файл и добавьте токен:

```env
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

---

## 🚀 Настройка для Production

Тот же токен (или новый) нужно добавить в Environment Variables в Vercel:

1. Vercel Dashboard → ваш проект → **Settings** → **Environment Variables**
2. Добавьте переменную:
   - **Name**: `BLOB_READ_WRITE_TOKEN`
   - **Value**: ваш токен
   - **Environment**: Production, Preview, Development (все)
3. Сохраните

---

## ✅ Проверка работы

### Локально:

```bash
npm run dev
```

Попробуйте загрузить файл через админ-панель. Файл должен загрузиться в Blob Store и вернуть публичный URL.

### На Production:

После деплоя на Vercel, файлы также будут загружаться в Blob Store.

---

## 🔧 Структура Blob Store

Файлы в Blob Store хранятся с путями:
- `images/product-123.jpg` - изображения продуктов
- `logos/logo.png` - логотипы
- `uploads/file.pdf` - другие загруженные файлы

Все файлы имеют публичный доступ и доступны по URL вида:
```
https://[blob-store-id].public.blob.vercel-storage.com/images/product-123.jpg
```

---

## ⚠️ Важно

- **Токен обязателен** для работы проекта (и локально, и на production)
- Без токена загрузка файлов не будет работать
- Токен можно использовать для нескольких окружений (локальное, preview, production)
- Или создать отдельные токены для каждого окружения (рекомендуется для production)

---

## 📝 Пример .env

```env
NODE_ENV=development
DATABASE_URL="postgresql://..."
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-here
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

---

**Готово!** Теперь проект работает с Blob Store в обоих окружениях. 🎉
